# Terraform configuration for SCM-MAX React App
# This file ensures project isolation

# Disable plugin caching to avoid conflicts
plugin_cache_dir = ".terraform/plugins"

# Use local state for development (change to S3 for production)
# terraform {
#   backend "s3" {
#     bucket = "your-terraform-state-bucket"
#     key    = "scm-max-react-app/terraform.tfstate"
#     region = "us-east-1"
#   }
# }
